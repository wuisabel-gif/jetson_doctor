# Build output
build/
cmake-build-*/

# Generated reports / logs (keep the directories, ignore the contents)
reports/*.html
logs/*.json
logs/*.csv

# Build spec / agent prompt — kept local, not part of the deliverable
AGENT.md

# Editor / OS noise
.DS_Store
*.swp
.vscode/
.idea/
